const menuItemDropDow = document.querySelectorAll(".menu-item-dropdow");

menuItemDropDow.forEach((menuItem)=>{
    menuItem.addEventListener('click'()=>{
        const subMenu = menuItem.querySelector('.sub-menu');
        menuItem.class  {
            constructor(parameters) {
                
            }
        }
    });

});